% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polyPlot.R
\name{polyPlot}
\alias{polyPlot}
\title{Modified PolyPlot Visualization}
\usage{
polyPlot(
  x,
  type = "none",
  main = NULL,
  xlab = NULL,
  ylab = NULL,
  xlim = NULL,
  ylim = NULL,
  values = TRUE,
  col = "black",
  bg = "gray60",
  cex = 1.2,
  digits = 2
)
}
\arguments{
\item{x}{A (non-empty) numeric vector of data values.}

\item{type}{Character string specifying what to plot in the background. Options are:
\itemize{
\item \code{"none"} (default): no background plot,
\item \code{"frequency"}: adds a histogram,
}}

\item{main}{Main title for the plot. If \code{NULL}, a default is generated based on \code{type}.}

\item{xlab}{Label for the x-axis. If \code{NULL}, the variable name is used.}

\item{ylab}{Label for the y-axis. If \code{NULL}, a default is used based on \code{type}.}

\item{xlim}{Numeric vector of length 2 for custom x-axis limits.}

\item{ylim}{Numeric vector of length 2 for custom y-axis limits.}

\item{values}{Logical; whether to display numerical values on the plot. Default is \code{TRUE}.}

\item{col}{Color used for the plot points, labels, and segments. Default is \code{"black"}.}

\item{bg}{Background color for the histogram or density polygon. Default is \code{"gray60"}.}

\item{cex}{Scaling factor for point symbols. Default is \code{1.2}.}

\item{digits}{Number of decimal places to use when displaying numeric labels. Default is \code{2}.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect of producing a plot.
}
\description{
Generates an enhanced polyplot for a numeric vector, overlaying key descriptive statistics (range, quartiles, mean/SD, trimmed mean/SD) and optionally displaying a histogram or density curve in the background.
}
\details{
The plot overlays four statistical layers, each represented by a line and three key values:
\enumerate{
\item \strong{Range}: Minimum, mean, and maximum.
\item \strong{Quartiles}: Q1, median, and Q3.
\item \strong{Mean/SD}: Mean and one standard deviation around it.
\item \strong{Trimmed Mean/SD}: Trimmed mean and one trimmed standard deviation.
}
Segments are drawn to connect the corresponding statistics across layers to emphasize shape and skewness. Background can include a histogram (frequency) or a density curve (density).
}
\examples{
set.seed(42)
x <- rnorm(100)
polyPlot(x, type = "density", col = "blue", bg = "lightblue")

}
\seealso{
\code{\link{polyStats}} for the underlying statistics used in plotting.
}
